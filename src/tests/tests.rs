use libcrypt::encrypt::*;
use libcrypt::decrypt::*;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert!(1 + 1 == 2);
    }

    #[test]
    fn enc_dec() {
        assert!(true);
    }
}
